services:
  - type: web
    name: urei-api
    runtime: python
    plan: free
    buildCommand: python -m pip install --upgrade pip && python -m pip install -r requirements.txt
    startCommand: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: FLASK_CONFIG
        value: production
      - key: FLASK_APP
        value: wsgi:app
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        value: https://urei-chi.vercel.app
      - key: AUTO_CREATE_DB
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: urei-db
          property: connectionString

databases:
  - name: urei-db
    plan: free
